import { FC } from "react";
interface ISublistProps {};

export const Sublist: FC<ISublistProps> = (props) => {
    return (
        <div className="Sublist">
            <h1 className="subList">so epic!!!!!!!</h1>
        </div>
    );
}
